{
  "name": "dsa-tracker",
  "version": "1.0.0",
  "description": "DSA problem tracker from Narasimha Karumanchi's book",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently -k \"node server.js\" \"cd client && npm run dev\"",
    "start": "node server.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.0",
    "@vercel/kv": "^3.0.0",
    "cookie-session": "^2.1.1",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "express-session": "^1.17.0",
    "passport": "^0.7.0",
    "passport-google-oauth20": "^2.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
